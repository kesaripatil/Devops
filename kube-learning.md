
############################ Therotical #########################
# Hypervisor is the brain or the core engine behind virtualization

# Types of Hypervisors:-
## 1. Type 1 virtualization: Bare metal - On os installed or virtualization layer is installed on hardware and given direct access to hardware.

## 2. Type 2 virtualization: Using virtualization apps on OS - The virtualization apps are installed like VMware workstation, Virtual box (Oracle), Hyper-V (Microsoft)

# KVM - Kernel based virtual machine - The tool installed on linux system which enables the kernel level virtualization. It uses tools like QEMU, libvirt.

######################## Commands  ################################
#Docker pull cmd to pull image from repository
1. docker image pull nginx:alpine
2. docker image pull alpine


