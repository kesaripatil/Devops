## Description
"Ngrok" is a utility which helps to establish the secure tunnel between the public endpoint to the locally running service or application.
It lets to expose the local service to the outside world without actually deploying it over the public server.

## How it works
1. ngrok client agent runs on local machine. 
